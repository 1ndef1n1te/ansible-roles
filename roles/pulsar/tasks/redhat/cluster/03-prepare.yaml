---
- name: Ensure Java is installed
  ansible.builtin.yum:
    name: java-{{ pulsar_java_version }}-headless
    state: present

- name: Create custom fact directory
  ansible.builtin.file:
    path: /etc/ansible/facts.d
    state: directory
    mode: "0755"
