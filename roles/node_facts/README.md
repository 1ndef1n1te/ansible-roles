Для работы роли на управляющем хосте должен быть установлен `jq`

Для формирования таблицы в md формате нужно запустить скрипт `facts_to_table.py` с ключами `--input: факты в json формате` `--output: название итогового файла с таблицей`