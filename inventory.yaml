---
all:
  children:
    nodes:
      children:
        k8s-nodes:
          children:
            k8s-masters:
              hosts:
            k8s-workers:
              hosts:
        opo-nodes:
          children:
            docker_nodes:
              hosts:
                docker-01:
                  ansible_ssh_host:
            elasticsearch-nodes:
              hosts:
                elasticsearch-01:
                  ansible_ssh_host:
            rabbitmq-nodes:
              hosts:
                rabbitmq-01:
                  ansible_ssh_host:
            mongodb_nodes:
              hosts:
            arangodb_nodes:
              hosts:
                arangodb-01:
                  ansible_ssh_host:
        repo-node:
          hosts:
  vars:
    ansible_ssh_user:
    ansible_ssh_port:
    ansible_ssh_pass:
    ansible_sudo_pass:
